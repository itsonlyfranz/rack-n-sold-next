'use client';

import { ConnectButton } from '@/components/wallet/connect-button';

/**
 * Simple wrapper for the ConnectButton to use in the navigation
 */
export function ConnectWalletButton() {
  return <ConnectButton />;
} 